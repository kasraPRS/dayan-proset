import { NationalCodeValidatorDirective } from "./national-code-validator.directive";

describe("NationalCodeValidatorDirective", () => {
  it("should create an instance", () => {
    const directive = new NationalCodeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
