import { CharacterAndNumberValidatorDirective } from "./character-and-number-validator.directive";

describe("CharacterAndNumberValidatorDirective", () => {
  it("should create an instance", () => {
    const directive = new CharacterAndNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
