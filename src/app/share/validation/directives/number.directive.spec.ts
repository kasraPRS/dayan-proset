import { NumberValidatorDirective } from "./number-validator.directive";

describe("NumberDirective", () => {
  it("should create an instance", () => {
    const directive = new NumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
