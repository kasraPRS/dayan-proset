export enum CustomIcon {
  close = "close",
  warning = "warning",
  info = "info",
  list = "list",
  chart = "chart",
  kanban = "kanban",
  history = "history",
  back = "back",
  print = "print",
}

export enum ToastType {
  SUCCESS = "SUCCESS",
  ERROR = "ERROR",
  WARNING = "WARNING",
  INFO = "INFO",
}
